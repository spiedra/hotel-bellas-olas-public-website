import { createTheme } from '@mui/material/styles'

export const theme = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#00253c'
    },
    secondary: {
      main: '#229148'
    },
    background: {
      default: '#fff',
      paper: '#fff'
    }
  }
})
