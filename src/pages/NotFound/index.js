import { Box } from '@mui/system'
import React from 'react'

const NotFound = () => {
  return <Box sx={{ mt: '3rem', ml: '1.5rem' }}>
    <h1>This is the not found</h1>
  </Box>
}

export default NotFound
