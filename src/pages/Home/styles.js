import styled from '@emotion/styled'

export const Div = styled.div((props) => ({
  display: 'flex',
  flex: '1',
  marginTop: '40px'
}))

export const Img = styled.img((props) => ({
  height: '400px',
  width: '400px'
}))
